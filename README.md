# Python Greeter App

A simple Python application containerized with Docker, featuring multi-branch version control using Git.

## Project Structure
- `app.py`: The main Python application script.
- `Dockerfile`: Configuration for containerizing the application.

## How to Build and Run with Docker

1. **Build the Docker image:**
   ```bash
   docker build -t greeter:1.0 .